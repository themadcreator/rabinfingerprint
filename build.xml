<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project default="build-cleanup" name="Create distributable jar">
	<!-- build input -->
	<property name="main.class" value="org.rabinfingerprint.Main" />
	<property name="jar.name" value="rabinfingerprint.jar" />
	<property name="src.dir" value="${basedir}/src" />
	<property name="lib.dir" value="${basedir}/lib" />

	<!-- build output -->
	<property name="dist.dir" value="${basedir}/dist" />
	<property name="build.dir" value="${basedir}/build" />

	<target name="clean">
		<delete dir="${dist.dir}" failonerror="false" />
		<delete dir="${build.dir}" failonerror="false" />
	</target>

	<target name="compile" depends="clean">
		<mkdir dir="${build.dir}" />
		<javac destdir="${build.dir}">
			<src path="${src.dir}" />
			<classpath>
				<fileset dir="${lib.dir}" includes="**/*.jar" />
			</classpath>
		</javac>
	</target>

	<target name="unjar_deps" depends="compile">
		<unzip dest="${build.dir}">
			<fileset dir="${lib.dir}">
				<include name="**/*.jar" />
			</fileset>
		</unzip>
	</target>

	<target name="build" depends="compile,unjar_deps">
		<mkdir dir="${dist.dir}" />
		<jar destfile="${dist.dir}/${jar.name}" duplicate="preserve">
			<manifest>
				<attribute name="Main-Class" value="${main.class}" />
				<attribute name="Class-Path" value="." />
			</manifest>
			<fileset dir="${build.dir}" />
		    <fileset dir="${src.dir}" includes="**/*.java"/>
		</jar>
	</target>

	<target name="build-cleanup" depends="build">
		<delete dir="${build.dir}" failonerror="false" />
	</target>
</project>
